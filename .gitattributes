*.pptx filter=lfs diff=lfs merge=lfs -text
*.pdf filter=lfs diff=lfs merge=lfs -text
*.docx filter=lfs diff=lfs merge=lfs -text
*.doc filter=lfs diff=lfs merge=lfs -text
*.png filter=lfs diff=lfs merge=lfs -text

# Module-3 lesson images extracted from source decks: committed as plain
# blobs (small, ~50-370 KB each) so they can be pushed from Claude Code
# sandboxes where LFS upload is not available. See docs/journal.md
# (2026-05-20: Module 3 extracting source-deck images post-hoc).
courses/aviation-weather/module-3/lessons/Images/*.png -filter -diff -merge
